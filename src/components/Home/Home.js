import React from 'react';
import Post from '../Post/Post';

const Home = () => {
    return (
        <div>
            <h2>This is Home</h2>
            <button>Sign in Google</button>
            <Post></Post>
        </div>
    );
};

export default Home;